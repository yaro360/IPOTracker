import React from 'react';
import ReactDOM from 'react-dom/client';
import IPOTrackerApp from './IPOTracker';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IPOTrackerApp />
  </React.StrictMode>
);
